using System;

namespace Sorting
{
    struct sampleStrucutre : System.IFormattable
    {
        public string ToString(string format, IFormatProvider formatProvider)
        {
            throw new NotImplementedException();
        }
    }
}